@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager

@model RequestForProvider

@{
    ViewBag.Title = "Provider Request";
}

<h1>Provider Request</h1>


    <!-- @if (SignInManager.IsSignedIn(User))
        {
        <div>
            User Id : @User.Identity.Name
        </div>
        } -->
<form method="post">
         
        <div class="form-group">
        <label>UserId</label>
        <input asp-for="@Model.Id" type="hidden" />
          @if (SignInManager.IsSignedIn(User))
        {
         <input asp-for= "@User.Identity.Name"  class="form-control" />
        }
        </div>
        <div class="form-group">
            <label>ServiceName</label>
            <select asp-for="@Model.ServiceName" class="form-control" >
             <option>Select</option>
               <option>Electrician</option>
               <option>A.C Mechanic</option>
               <option>Generator Mechanic</option>
        </select>
         </div>
          <button type="submit" class="btn btn-primary">Submit</button>
    </form>